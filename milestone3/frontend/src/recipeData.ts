export interface Recipe {
    name: string;
    description: string;
    image: string;
    ingredients: string[];
    instructions: string[];
}

const recipes: Recipe[] = [
    //objects of recipes from milestone1 folder
    
];

export default recipes; // This will allow us to access this data anywhere!