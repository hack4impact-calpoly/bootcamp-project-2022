export interface Recipe {
    name: string;
    description: string;
    image: string;
    ingredients: string[];
    instructions: string[];
  }

const recipes: Recipe[] = [
	{recipeObject},
	{recipeObject},
	{recipeObject} 
];

export default recipes;